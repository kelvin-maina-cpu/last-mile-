# Frontend API origin. The app appends /api internally.
VITE_API_URL=http://localhost:3000

# Optional Socket.IO override. Leave unset to derive from VITE_API_URL.
VITE_WS_URL=

# Development-only demo toggles.
VITE_USE_MOCK_AUTH=false
VITE_USE_MOCK_DATA=false
